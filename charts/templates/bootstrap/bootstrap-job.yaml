{{- if .Values.bootstrap.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: "{{ .Release.Name }}-bootstrap"
  labels:
    app.kubernetes.io/name: {{ include "dependencytrack.name" . }}-bootstrap
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: bootstrap
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    helm.sh/chart: {{ include "dependencytrack.chart" . }}
  annotations:
    # This is what defines this resource as a hook. Without this line, the
    # job is considered part of the release.
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  ttlSecondsAfterFinished: 60
  backoffLimit: 6
  template:
    metadata:
      name: "{{ .Release.Name }}-bootstrap"
      labels:
        app.kubernetes.io/name: {{ include "dependencytrack.name" . }}-bootstrap
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: bootstrap
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        helm.sh/chart: {{ include "dependencytrack.chart" . }}
    spec:
      restartPolicy: Never
      securityContext:
        seccompProfile:
          type: RuntimeDefault
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: "{{ .Release.Name }}-bootstrap"
          image: "{{ .Values.bootstrap.image.repository }}/{{ .Values.bootstrap.image.name }}:{{ .Values.bootstrap.image.tag }}"
          volumeMounts:
            - name: bootstrap
              mountPath: "/bootstrap"
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          env:
            - name: LOG_LEVEL
              value: "{{ .Values.bootstrap.logLevel }}"
            - name: BASE_URL
              value: "{{ .Values.bootstrap.baseUrl }}"
            - name: GOOGLE_OSV_ENABLED
              value: "{{ .Values.bootstrap.google.osv_enabled }}"
            - name: FRONTEND_BASE_URL
              value: "{{ .Values.bootstrap.frontendBaseUrl }}"
            - name: TRIVY_IGNORE_UNFIXED
              value: "{{ .Values.bootstrap.trivy.ignore_unfixed }}"
          {{- if .Values.bootstrap.trivy.base_url }}
            - name: TRIVY_BASE_URL
              value: "{{ .Values.bootstrap.trivy.base_url }}"
            {{- end }}
          {{- if .Values.bootstrap.adminPasswordSecret.name }}
            - name: ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.bootstrap.adminPasswordSecret.name }}
                  key: {{ .Values.bootstrap.adminPasswordSecret.key }}
          {{- end }}
          {{- if .Values.bootstrap.github.advisoryMirroringTokenSecret.name }}
            - name: GITHUB_ADVISORY_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.bootstrap.github.advisoryMirroringTokenSecret.name }}
                  key: {{ .Values.bootstrap.github.advisoryMirroringTokenSecret.key }}
          {{- end }}
          {{- if .Values.bootstrap.trivy.apiTokenSecret.name }}
            - name: TRIVY_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.bootstrap.trivy.apiTokenSecret.name }}
                  key: {{ .Values.bootstrap.trivy.apiTokenSecret.key }}
          {{- end }}
          {{- if .Values.bootstrap.oss_index.tokenSecret.name }}
            - name: OSS_INDEX_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.bootstrap.oss_index.tokenSecret.name }}
                  key: {{ .Values.bootstrap.oss_index.tokenSecret.key }}
          {{- end }}

          envFrom:
            - secretRef:
                name: "{{ .Release.Name }}-bootstrap-config"
      volumes:
        - name: bootstrap
          secret:
            secretName: "{{ .Release.Name }}-bootstrap-mount"
{{- end }}
